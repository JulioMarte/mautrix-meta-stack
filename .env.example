# Copy to .env only for local/SSH bootstrap use. In Coolify, define these as environment variables.
MATRIX_SERVER_NAME=matrix.example.com
MATRIX_ADMIN_MXID=@admin:matrix.example.com
TZ=America/Santo_Domingo

# Persistent host path. Must exist before deployment; scripts/bootstrap.sh creates it.
STACK_DATA_ROOT=/data/mautrix-meta-stack

# Pin releases. Do not use latest in production.
SYNAPSE_IMAGE=matrixdotorg/synapse:v1.160.0
MAUTRIX_META_IMAGE=dock.mau.dev/mautrix/meta:v26.07
